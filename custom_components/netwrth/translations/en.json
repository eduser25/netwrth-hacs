{
  "config": {
    "step": {
      "user": {
        "title": "Connect to netwrth",
        "description": "Create an API key in netwrth under Settings → Integrations, then paste it here. A censored-only key never exposes real amounts to Home Assistant.",
        "data": {
          "api_key": "API key"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach netwrth.",
      "invalid_auth": "netwrth rejected the API key."
    },
    "abort": {
      "already_configured": "This netwrth API key is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "netwrth options",
        "data": {
          "scan_interval_minutes": "Polling interval (minutes)",
          "reveal_ttl_minutes": "Default auto-conceal after reveal (minutes, 0 = stay revealed)",
          "expose_real_sensors": "Expose real-currency sensors (writes real amounts into HA history while revealed)"
        }
      }
    }
  }
}
